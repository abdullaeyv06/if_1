let ask = prompt("what is your name") .toUpperCase()

if (ask[0].toUpperCase() == "A") {
     let age = prompt("how old are you")
    if (age >=20 && age <40  ) {
        let money = prompt("how much do you have")
        if (money >= 100) {
          let   ask_num = prompt("how many are you")
        }else {
            alert("error")
        }
        
    }else {
        alert("error")
    }
    
} else {
    
    alert("error")
}